##  Каталог проекта битрикс.

Необходимые корректировки:

1. Установить проект из резервной копии;
2. Удалить каклог `local` в проекте;
3. Создать символическую ссылку `local` на каталог `local` второго урока.

